<div class="top_page">
  <div class="list_wrapper">
    <% @lists.each do |list| %>
      <div class="list">
        <div class="list_header">
          <h2 class="list_header_title"><%= list.title %></h2>
          <div class="list_header_action">
            <%= link_to(content_tag(:i, '', class: 'fas fa-pen'), edit_list_path(list)) %>   
            <%= link_to content_tag(:i, '', class: 'fas fa-trash'), list, method: :delete, data: { confirm: "#{list.title}を削除して大丈夫ですか？" } %>
          </div>          
        </div>

        <%# ここからカード %>
        <div class="card_wrapper">
          <% list.cards.each do |card| %>
            <div class="card">
              <h3 class="card_title"><%= card.title %></h3>
            </div>
          <% end %>
          <div class="card_add">
            <i class="far fa-plus-square"></i>
              <%= link_to "さらにカードを追加", new_list_card_path(list), class: "addCard_link" %>
          </div>
        </div>

    <% end %>
  </div>
</div>



<%# # blog(@list)が空(@list = List.newな状態)の場合は、url: list_path, method: postを自動推測、つまりListControllerのcreateアクションにデータを送る
list(@list)が空でない(@list = List.find(〇)な状態)場合は、url: edit_lidt_pathを自動推測、つまりListControllerのeditアクションにデータを送る  %>